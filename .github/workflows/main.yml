name: Manual Trigger Demo

# Trigger manually
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Your Name'
        required: true

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello ${{ github.event.inputs.name }}! Welcome to GitHub Actions."
